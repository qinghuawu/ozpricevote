package disCache

import (
	"testing"
)

func TestHTTPPool_ServeHTTP(t *testing.T) {
	//addr := "localhost:9999"
	//peers := NewHTTPPool(addr)
	//disCache = NewCache(getFromDB, 100)
	//log.Println("disCache is running at", addr)
	//log.Fatal(http.ListenAndServe(addr, peers))
}
